const PUBLISHED_COPY={"header-title": "k编舞两周年纪念册", "header-above-0": "2024.9.16～2026.9.16", "header-above-1": "K编舞两年回忆录", "header-badge": "\n", "dance-date-0": "2024.9.16", "dance-song-0": "Paris", "dance-date-2": "2026.4.16", "dance-song-2": "Need It", "dance-date-1": "2026.1.6", "dance-date-3": "2026.7.24", "dance-song-1": "Love Don't Cost A Dime", "dance-song-3": "Don't Jealous Me", "dance-description-0": "第一次上课的时候大概是我瞎跳了一年舞，认认真真上了三个月爵士之后，因为台风天没课上去上了你的课。那个时候跳舞爱站第一排，感觉老师好严格，一节课纠正我动作n遍，但是也很意外最后居然会有一个被捡的合拍，现在看跳的真惨不忍睹。", "first-class-story-20260914-original": true, "dance-description-1": "这是第一次被你push上去的solo，严重偏台且严重不自信，但那个时候感觉自己跳舞发力的方式改变了，是有在慢慢进步吧。", "second-dance-story-20260914-original": true, "dance-description-2": "这支舞是你修养好膝盖回来不久后学的，这一支可能是我两年k编舞以来唯一满意的一支，当时也没觉得多好看，记忆犹新的是下课被球球夸了，当天晚上发dy还被室友夸了，然后我就复盘了一下，好像确实还可以，甚至目前看了就这支舞算得上是自己比较满意的。", "third-dance-story-20260914-original": true, "dance-description-3": "这支舞是我最最最喜欢的jealous，这支舞从开新，几乎是一天学一次的，而且也是从这支舞开始，我变得自信了，我开始可以自信的站在c位拍视频了，也可以自愿上去solo了，这次solo其实没有特别冷静，我感觉没有小组跳跳得好，但你说solo跳不好没事，solo是用来练胆量的。所以从那之后，只要有solo的机会，且动作没有记得不太熟的情况下，我都会勇敢走上去。", "fourth-dance-story-20260914-original": true, "copy-4": "第一张合照", "copy-8": "K编舞700天", "copy-6": "我俩一样矮", "copy-10": "最近很喜欢的一张", "copy-5": "去年你生日才有的第一张合照，因为我真的很i，我更不敢主动跟老师拍合照，但是那天你生日，每个人都可以轮流拍合照，所以我也有了第一张合照。", "copy-9": "不敢相信，这居然是本i人第一次主动提出跟你合照，700天才敢哈哈哈哈哈，是真的很内向一个小孩了。", "copy-7": "最近脸皮厚了，也敢跟你拍合照了，频率还不低，而且跟姐拍照你也得蹲下。", "copy-11": "这个表情简直了！很在我的点上，拍的时候没注意，拍完发现你这个表情营业得我非常之满意，好喜欢这张照片！", "header-subtitle": "", "header-title-k-anniversary-20260915": true, "copy-0": "致亲爱的KK老师", "personal-letter-20260915": "其实有很多话想说，但不知道从哪里说起。我在舞房里一直是一个很i的人，我不会主动跟人讲话，但其实生活里并没有那么内向，所以我觉得是因为我自己在跳舞这件事情上一直都是非常非常不自信的，也没有想过我要跳得有多好，一开始就是为了减肥来的。但自从上了你的课之后，我认识了你，认识了很多你课上的学生，我会想要跟大家一起进步，我有了要把舞跳好看这个想法！虽然距离第一次上你的课已经过去了两年了，但我一开始就是瞎上，也跳不明白编舞，去年6月才正式开始跟着你跑课，但感觉除了你的课之外，我还是跳爵士比较多，所以也会觉得自己进步很慢吧，跳舞也还是不好看。到今年的年初，我才开始慢慢的去接触更多的编舞老师（但其实依旧是今年毕业前夕在p办时限卡之后，才push自己去上课的），让自己去习惯不同的元素和不同的发力，看到的多了，学到的也会更多一点吧，但与此同时也发现了自己跳舞存在的问题，对自己的要求也越来越高了吧。最近这段时间也感觉自己越来越勇敢，拍视频站c开始不紧张了，solo也会push自己上，真的很感谢你给我的能量吧！\n虽然说目前我在你的课上跳得不算好（有时候也会后悔自己编舞跳太少了，p办卡办得太晚了，在p跳舞真的进步会更快一点吧哈哈哈哈），但我还是会继续努力的，我希望有一天我也可以跳得很好！当然我也会一直感谢你给过我的帮助！这个k编舞一直跳下去吧！\n当然还要特别感谢你是一个很细心的人，你会去关注学生的情绪，让大家开开心心跳舞。应该有很多人感谢过你给予的能量，所以我今天更想说的是，每个人的能量都有限，你完全可以留一部分能量给你自己，我希望你也可以更开心的只做你自己。", "letter-signature-jia-20260915": true, "letter-signature-from-jia-20260915": true, "letter-paragraph-repair-20260915": true, "letter-three-original-paragraphs-v2": true};
const PUBLISHED_PLACEMENT={"moment-3": {"x": 1, "y": 3, "cropX": 50, "cropY": 100}, "moment-2": {"cropX": 50, "cropY": 100}, "moment-1": {"cropX": 50, "cropY": 100, "x": 2, "y": 9}, "moment-0": {"cropX": 50, "cropY": 63.043477043478255}};
const spreads=[...document.querySelectorAll('[data-spread]')],nav=[...document.querySelectorAll('[data-page]')];let page=0,clip=0,chosenPhoto=null;const clips=['14.mp4','16.mp4','17.mp4','15.mp4'];
function scheduleWallPlayback(active){
 (scheduleWallPlayback.timers||[]).forEach(clearTimeout);scheduleWallPlayback.timers=[];
 if(!active)return;
 const videos=[...document.querySelectorAll('.letter video')];
 videos.forEach((video,index)=>{scheduleWallPlayback.timers.push(setTimeout(()=>{
  if(page!==6||document.hidden)return;
  video.play().catch(()=>{});
 },850+index*180))});
}
function showPage(n){const previous=page;page=Math.max(0,Math.min(8,n));const section=page===0?0:page<=4?2:page===5?4:page===6?1:page===7?3:5;spreads.forEach((s)=>{const i=+s.dataset.spread;s.hidden=i!==section;s.classList.remove('turning');if(i===section){s.style.setProperty('--turn-direction',page<previous?'5deg':'-5deg');void s.offsetWidth;s.classList.add('turning')}});nav.forEach(b=>{const active=+b.dataset.page===(section===2?1:page);b.classList.toggle('active',active);b.setAttribute('aria-current',active?'page':'false')});for(const id of ['prev','corner-prev'])document.getElementById(id).disabled=page===0;for(const id of ['next','corner-next'])document.getElementById(id).disabled=page===8;document.querySelector('#page-count').textContent=`0${page} — 08`;document.querySelector('.dots').innerHTML=Array.from({length:9},(_,i)=>`<i class="${i===page?'active':''}"></i>`).join('');document.querySelectorAll('.moment-video video').forEach(v=>{v.muted=true;if(section===4){v.play().catch(()=>{});}else v.pause()});document.querySelectorAll('.letter video').forEach(v=>v.pause());scheduleWallPlayback(section===1);if(section===2)setClip(page-1);else document.querySelector('#video').pause()}
nav.forEach(b=>b.onclick=()=>showPage(+b.dataset.page));document.querySelector('#begin').onclick=()=>showPage(1);document.querySelector('#prev').onclick=()=>showPage(page-1);document.querySelector('#next').onclick=()=>showPage(page+1);document.querySelector('#corner-prev').onclick=()=>showPage(page-1);document.querySelector('#corner-next').onclick=()=>showPage(page+1);document.addEventListener('keydown',e=>{if(document.querySelector('dialog').open||['INPUT','VIDEO','BUTTON'].includes(document.activeElement.tagName))return;if(e.key==='ArrowRight')showPage(page+1);if(e.key==='ArrowLeft')showPage(page-1)});
const tones=['#8e9b95','#b1a18f','#a8aaa0','#927d75','#b7b09e','#7c8984','#a59587','#b2a38b','#8e9187'];const coordinates=[[8,0,0],[8,21,0],[8,42,0],[8,63,0],[8,84,0],[38,44,42],[65,24,42],[38,63,-42],[65,84,-42]];
['left-k','right-k'].forEach((id,side)=>{const parent=document.getElementById(id);coordinates.forEach(([x,y,rotation],i)=>{const b=document.createElement('button');b.className='photo';b.dataset.slot=String(i+1);b.style.cssText=`left:${x}%;top:${y*.83}%;--rot:${rotation}deg;--tone:${tones[(i+side*3)%tones.length]}`;const num=String(side*9+i+1).padStart(2,'0');b.innerHTML=`<span class="photo-face">${num}</span><small>回忆 ${num}</small>`;const source=(side===0?['3.jpg','4.jpg','5.jpg','4.mp4','5.mp4','6.mp4','7.mp4','8.mp4','8.jpg']:['2.jpg','7.jpg','12.mp4','10.mp4','11.mp4','9.mp4','13.mp4','9.jpg','10.jpg'])[i];if(source){b.photoURL=''+source;b.isVideo=source.endsWith('.mp4');b.dataset.asset=source;if(['2.jpg','5.jpg','7.jpg'].includes(source))b.classList.add('landscape-photo');const media=b.isVideo?document.createElement('video'):new Image();media.src=b.photoURL;if(b.isVideo){media.muted=true;media.loop=true;media.playsInline=true;media.preload='metadata';media.setAttribute('aria-hidden','true')}else{media.alt=`回忆 ${num}`;media.loading='lazy'}b.querySelector('.photo-face').replaceChildren(media)}b.setAttribute('aria-label',source?`放大回忆 ${num}`:`回忆 ${num}，待补充`);b.onclick=()=>{chosenPhoto=b;document.querySelector('#photo-title').textContent=`回忆 ${num}`;const large=document.querySelector('#photo-large');large.replaceChildren();if(b.photoURL){if(b.isVideo){const v=document.createElement('video');v.src=b.photoURL;v.muted=true;v.loop=true;v.playsInline=true;large.append(v);v.play().catch(()=>{})}else{const img=new Image();img.src=b.photoURL;img.alt=`回忆 ${num}`;large.append(img)}}else large.textContent=num;document.querySelector('#photo-dialog').showModal()};parent.append(b)})});
const dialog=document.querySelector('dialog');document.querySelector('.close').onclick=()=>dialog.close();dialog.addEventListener('click',e=>{if(e.target===dialog){const r=dialog.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)dialog.close()}});document.querySelector('#photo-file').onchange=e=>{const f=e.target.files[0];if(!f||!chosenPhoto||!f.type.startsWith('image/'))return;if(chosenPhoto.photoURL)URL.revokeObjectURL(chosenPhoto.photoURL);chosenPhoto.isVideo=false;chosenPhoto.photoURL=URL.createObjectURL(f);for(const el of[chosenPhoto.querySelector('.photo-face'),document.querySelector('#photo-large')]){const img=new Image();img.src=chosenPhoto.photoURL;img.alt='选入的回忆照片';el.replaceChildren(img)}e.target.value=''};
function setClip(n){clip=n;const portrait=false;document.querySelector('.video-spread').classList.toggle('portrait',portrait);document.querySelector('#video-placeholder p').textContent=portrait?'竖屏视频 · 保留完整舞蹈画面':'横屏视频 · 完整跨越左右两页';document.querySelectorAll('[data-video]').forEach((b,i)=>{b.classList.toggle('active',i===n);b.setAttribute('aria-pressed',String(i===n))});const v=document.querySelector('#video');v.pause();v.hidden=!clips[n];document.querySelector('#video-placeholder').hidden=!!clips[n];document.querySelector('#video-placeholder').style.display=clips[n]?'none':'flex';if(clips[n])v.src=clips[n];else v.removeAttribute('src');v.load();document.querySelector('.video-number').textContent=`0${n+1}`;document.querySelector('#dance-label').textContent=`舞蹈 0${n+1}`;document.querySelector('#dance-title').textContent=['这里写这支舞的名字','第二支舞的名字','第三支舞的名字','第四支舞的名字'][n]}
document.querySelectorAll('[data-video]').forEach(b=>b.onclick=()=>showPage(+b.dataset.video+1));document.querySelector('#video-file').onchange=e=>{const f=e.target.files[0];if(!f||!f.type.startsWith('video/'))return;if(clips[clip])URL.revokeObjectURL(clips[clip]);clips[clip]=URL.createObjectURL(f);setClip(clip);e.target.value=''};showPage(0);setClip(0);

// The four story photos share the same local-only image preview dialog.
document.querySelectorAll('[data-moment]').forEach(b=>{b.photoURL='1.jpg';b.onclick=()=>{chosenPhoto=b;const num=b.dataset.moment;document.querySelector('#photo-title').textContent=`瞬间 0${num}`;const large=document.querySelector('#photo-large');large.replaceChildren();if(b.photoURL){const img=new Image();img.src=b.photoURL;img.alt=`瞬间 ${num}`;large.append(img)}else large.textContent=`0${num}`;dialog.showModal()}});

dialog.addEventListener('close',()=>{dialog.querySelectorAll('video').forEach(v=>v.pause())});

// Match this frame to the actual video ratio so faces remain visible without bars.
document.querySelectorAll('.photo[data-asset="6.mp4"] video').forEach(v=>{
 const fit=()=>{if(v.videoWidth&&v.videoHeight)v.closest('.photo').style.setProperty('--media-ratio',`${v.videoWidth} / ${v.videoHeight}`)};
 v.addEventListener('loadedmetadata',fit);fit();
});
// Fit each frame to its source ratio within a non-overlapping collage slot.
function fitMemoryFrames(){
 document.querySelectorAll('.letter .photo').forEach(frame=>{
  const parent=frame.closest('.letter'),media=frame.querySelector('img,video');
  if(!parent.clientWidth)return;
  const sourceWidth=media&&(media.videoWidth||media.naturalWidth),sourceHeight=media&&(media.videoHeight||media.naturalHeight);
  const ratio=sourceWidth&&sourceHeight?sourceWidth/sourceHeight:3/4;
  if(parent.classList.contains('free-collage'))return;
  if(frame.dataset.manualWidth){const width=parent.clientWidth*Number(frame.dataset.manualWidth)/100;frame.style.setProperty('--frame-width',width+'px');frame.style.setProperty('--frame-height',width/(Number(frame.dataset.manualRatio)||ratio)+'px');return;}
  const angle=Math.abs(parseFloat(getComputedStyle(frame).getPropertyValue('--collage-angle'))||0)*Math.PI/180;
  const cosine=Math.cos(angle),sine=Math.sin(angle);
  const diagonal=Number(frame.dataset.slot)>=6;
  const width=Math.min(parent.clientWidth*(diagonal?.38:.33)/(cosine+sine/ratio),parent.clientHeight*(diagonal?.28:.215)/(sine+cosine/ratio));
  frame.style.setProperty('--frame-width',`${width}px`);
  frame.style.setProperty('--frame-height',`${width/ratio}px`);
 });
}
document.querySelectorAll('.letter img,.letter video').forEach(media=>{
 media.addEventListener(media.tagName==='VIDEO'?'loadedmetadata':'load',fitMemoryFrames);
});
const memoryResizeObserver=new ResizeObserver(fitMemoryFrames);
document.querySelectorAll('.letter').forEach(el=>memoryResizeObserver.observe(el));
fitMemoryFrames();

// Editable personal copy, stored only in this browser.
const editableSelectors=['.cover-left .intro','.cover-left h1','.bodycopy','.cover-note','.moment-copy h3','.moment-copy p','#dance-title','.video-caption p','.letter-right h3','.letter-right>p','.signature'];
let draftStore={};
try{draftStore=structuredClone(PUBLISHED_COPY)}catch{}
const editStatus=document.createElement('span');editStatus.className='edit-status';editStatus.setAttribute('role','status');editStatus.textContent='点击文字可编辑 · 自动保存在此浏览器';document.querySelector('header').append(editStatus);
function copyKey(el){return el.id==='dance-title'?'dance-title-'+clip:el.matches('.video-caption p')?'dance-description-'+clip:el.dataset.copyKey}
function saveCopy(el){draftStore[copyKey(el)]=el.classList.contains('personal-letter')?Array.from(el.childNodes).map(node=>node.textContent).join('\n'):el.innerText;try{void 0;editStatus.textContent='文字已保存在此浏览器'}catch{editStatus.textContent='此浏览器无法保存，请先复制备份文字'}}
const editableNodes=[...document.querySelectorAll(editableSelectors.join(','))];
editableNodes.forEach((el,i)=>{el.dataset.copyKey='copy-'+i;el.contentEditable='plaintext-only';el.setAttribute('role','textbox');el.setAttribute('aria-multiline','true');el.setAttribute('aria-label','可编辑文字');el.spellcheck=false;el.classList.add('editable-copy');if(draftStore[copyKey(el)]!==undefined)el.innerText=draftStore[copyKey(el)];el.addEventListener('input',()=>saveCopy(el));el.addEventListener('keydown',e=>e.stopPropagation())});
const defaultDanceDescription='这里可以讲讲为什么选这支舞、练习时的小故事，或是想用它对老师说的话。';
const originalSetClip=setClip;
setClip=function(n){originalSetClip(n);const title=document.querySelector('#dance-title'),desc=document.querySelector('.video-caption p');if(draftStore['dance-title-'+n]!==undefined)title.innerText=draftStore['dance-title-'+n];desc.innerText=draftStore['dance-description-'+n]??defaultDanceDescription};
document.querySelector('.sample').textContent='点击下方文字，写下你自己的话';
document.querySelector('.draft').textContent='我的两周年纪念册';

// Header copy uses independent keys so previously saved body text stays intact.
const brand=document.querySelector('.brand');
const brandTitle=document.createElement('span');brandTitle.textContent=brand.firstChild.textContent;brand.replaceChild(brandTitle,brand.firstChild);
const headerFields=[['header-title',brandTitle],['header-subtitle',brand.querySelector('small')],['header-badge',document.querySelector('.draft')],...Array.from(document.querySelectorAll('.above>span'),(el,i)=>['header-above-'+i,el])];
nav.forEach((button,i)=>{const label=document.createElement('span');label.textContent=button.textContent;button.replaceChildren(label);headerFields.push(['chapter-label-'+i,label]);label.addEventListener('click',e=>{e.stopPropagation();showPage(+button.dataset.page)})});
headerFields.forEach(([key,el])=>{el.dataset.copyKey=key;el.contentEditable='plaintext-only';el.classList.add('editable-copy');el.setAttribute('role','textbox');el.setAttribute('aria-label','编辑'+el.textContent);el.spellcheck=false;if(draftStore[key]!==undefined)el.innerText=draftStore[key];el.addEventListener('input',()=>saveCopy(el));el.addEventListener('keydown',e=>{e.stopPropagation();if(e.key==='Enter'){e.preventDefault();el.blur()}});el.addEventListener('click',e=>{if(el.closest('.brand'))e.preventDefault()})});

// Turn one half of the book around its spine, keeping the opposite page visible.
const bookElement=document.querySelector('#book');
document.querySelector('nav').hidden=true;
let pageTurnAnimation=null;
function freezeTurnPage(section){
 const copy=section.cloneNode(true);
 const originals=section.querySelectorAll('video,canvas'),copies=copy.querySelectorAll('video,canvas');
 originals.forEach((source,i)=>{
  const canvas=document.createElement('canvas');
  const sw=source.videoWidth||source.width||320,sh=source.videoHeight||source.height||180;
  const scale=Math.min(1,800/sw);canvas.width=Math.max(1,Math.round(sw*scale));canvas.height=Math.max(1,Math.round(sh*scale));
  canvas.className='frozen-turn-media';canvas.style.cssText=source.style.cssText;
  if(source.isConnected){const style=getComputedStyle(source);canvas.style.objectFit=style.objectFit;canvas.style.objectPosition=style.objectPosition;}
  try{canvas.getContext('2d').drawImage(source,0,0,canvas.width,canvas.height)}catch{}
  copies[i].replaceWith(canvas);
 });
 return copy;
}
function snapshotPage(section,width,height,offset){
 const panel=document.createElement('div');panel.className='turn-snapshot';
 const copy=freezeTurnPage(section);copy.hidden=false;copy.classList.remove('turning');copy.style.width=width+'px';copy.style.height=height+'px';copy.style.minHeight=height+'px';copy.style.position='absolute';copy.style.left=offset+'px';copy.style.top='0';
 copy.querySelectorAll('[id]').forEach(el=>el.removeAttribute('id'));
 copy.querySelectorAll('[contenteditable]').forEach(el=>el.removeAttribute('contenteditable'));
 copy.querySelectorAll('video').forEach(el=>{el.removeAttribute('autoplay');el.pause()});
 panel.append(copy);return panel;
}
const changeBookPage=showPage;
showPage=function(n){
 const target=Math.max(0,Math.min(8,n));if(target===page)return;
 if(pageTurnAnimation)return;
 const outgoing=spreads.find(s=>!s.hidden);const previous=page;
 const width=bookElement.clientWidth,oldHeight=bookElement.clientHeight;
 const oldCopy=freezeTurnPage(outgoing);
 changeBookPage(target);
 if(matchMedia('(prefers-reduced-motion: reduce)').matches)return;
 const incoming=spreads.find(s=>!s.hidden);incoming.classList.remove('turning');
 const height=Math.max(oldHeight,bookElement.clientHeight);const forward=target>previous;
 const layer=document.createElement('div');layer.className='physical-turn '+(forward?'forward':'backward');layer.style.height=height+'px';layer.setAttribute('aria-hidden','true');layer.inert=true;
 const resting=document.createElement('div');resting.className='turn-resting';resting.append(snapshotPage(oldCopy,width,height,forward?0:-width/2));
 const leaf=document.createElement('div');leaf.className='turn-leaf';
 const front=document.createElement('div');front.className='turn-face turn-front';front.append(snapshotPage(oldCopy,width,height,forward?-width/2:0));
 const back=document.createElement('div');back.className='turn-face turn-back';back.append(snapshotPage(incoming,width,height,forward?0:-width/2));
 incoming.querySelectorAll('video').forEach(v=>v.pause());
 leaf.append(front,back);layer.append(resting,leaf);bookElement.append(layer);
 const animation=leaf.animate([{transform:'rotateY(0deg)'},{transform:`rotateY(${forward?-180:180}deg)`}],{duration:720,easing:'cubic-bezier(.25,.1,.25,1)',fill:'forwards'});pageTurnAnimation=animation;
 const cleanup=()=>{layer.remove();if(pageTurnAnimation===animation)pageTurnAnimation=null;incoming.querySelectorAll('.moment-video video').forEach(v=>v.play().catch(()=>{}))};animation.onfinish=cleanup;animation.oncancel=cleanup;
};

// Direct manipulation of memory frames and their crop positions.
let placementStore={};try{placementStore=structuredClone(PUBLISHED_PLACEMENT)}catch{}
let mediaEditMode='view',dragState=null;
const mediaToolbar=document.createElement('div');mediaToolbar.className='media-edit-toolbar';mediaToolbar.setAttribute('aria-label','照片排版工具');
for(const [mode,label] of [['view','正常查看'],['move','移动相框'],['crop','调整画面']]){const button=document.createElement('button');button.textContent=label;button.type='button';button.dataset.mode=mode;button.onclick=()=>{mediaEditMode=mode;document.body.dataset.mediaEdit=mode;mediaToolbar.querySelectorAll('button').forEach(b=>b.setAttribute('aria-pressed',String(b.dataset.mode===mode)))};button.setAttribute('aria-pressed',String(mode==='view'));mediaToolbar.append(button)}
const hint=document.createElement('span');hint.textContent='拖动调整 · 保存在此浏览器';mediaToolbar.append(hint);document.querySelector('.book-controls').before(mediaToolbar);
const editableFrames=[...document.querySelectorAll('.letter .photo,.moment-photo')];
function placementKey(frame){return frame.closest('.letter')?frame.closest('.letter').id+'-'+frame.dataset.slot:'moment-'+[...document.querySelectorAll('.moment-photo')].indexOf(frame)}
function applyPlacement(frame,data){
 if(data.x!==undefined){if(frame.closest('.letter')){frame.style.setProperty('--layout-x',data.x+'%');frame.style.setProperty('--layout-y',data.y+'%')}else{frame.style.position='relative';frame.style.left=data.x+'px';frame.style.top=data.y+'px'}}
 const media=frame.querySelector('img,video');if(media&&data.cropX!==undefined)media.style.setProperty('object-position',`${data.cropX}% ${data.cropY}%`,'important');
}
editableFrames.forEach(frame=>{frame.dataset.placementKey=placementKey(frame);if(placementStore[frame.dataset.placementKey])applyPlacement(frame,placementStore[frame.dataset.placementKey]);frame.querySelectorAll('img,video').forEach(m=>m.draggable=false);
 frame.addEventListener('click',e=>{if(mediaEditMode!=='view'){e.preventDefault();e.stopImmediatePropagation()}},true);
 frame.addEventListener('pointerdown',e=>{
 if(mediaEditMode==='view'||e.button!==0)return;const media=frame.querySelector('img,video');if(mediaEditMode==='crop'&&!media)return;
 e.preventDefault();e.stopPropagation();const key=frame.dataset.placementKey,data={...(placementStore[key]||{})},parent=frame.closest('.letter');
 const style=getComputedStyle(frame),crop=media?getComputedStyle(media).objectPosition.split(' '):[];
 dragState={frame,media,key,data,parent,startX:e.clientX,startY:e.clientY,x:parent?frame.offsetLeft/parent.clientWidth*100:(data.x||0),y:parent?frame.offsetTop/parent.clientHeight*100:(data.y||0),cropX:parseFloat(crop[0])||0,cropY:parseFloat(crop[1])||0};
 frame.setPointerCapture(e.pointerId);frame.classList.add('dragging-frame');
 });
 frame.addEventListener('pointermove',e=>{
 if(!dragState||dragState.frame!==frame)return;const d=dragState,dx=e.clientX-d.startX,dy=e.clientY-d.startY;
 if(mediaEditMode==='move'){d.data.x=d.parent?Math.max(0,Math.min(100-frame.offsetWidth/d.parent.clientWidth*100,d.x+dx/d.parent.clientWidth*100)):d.x+dx;d.data.y=d.parent?Math.max(0,Math.min(100-frame.offsetHeight/d.parent.clientHeight*100,d.y+dy/d.parent.clientHeight*100)):d.y+dy}
 else{const angle=parseFloat(getComputedStyle(frame).getPropertyValue('--collage-angle'))||0,r=angle*Math.PI/180;const localX=dx*Math.cos(r)+dy*Math.sin(r),localY=-dx*Math.sin(r)+dy*Math.cos(r);const w=d.media.videoWidth||d.media.naturalWidth,h=d.media.videoHeight||d.media.naturalHeight,bw=d.media.clientWidth,bh=d.media.clientHeight,scale=Math.max(bw/w,bh/h),extraX=w*scale-bw,extraY=h*scale-bh;d.data.cropX=Math.max(0,Math.min(100,d.cropX-(extraX>1?localX/extraX*100:0)));d.data.cropY=Math.max(0,Math.min(100,d.cropY-(extraY>1?localY/extraY*100:0)))}
 applyPlacement(frame,d.data);
 });
 const finish=()=>{if(!dragState||dragState.frame!==frame)return;placementStore[dragState.key]=dragState.data;try{void 0;hint.textContent='排版已保存在此浏览器'}catch{hint.textContent='当前浏览器无法保存排版'}frame.classList.remove('dragging-frame');dragState=null};frame.addEventListener('pointerup',finish);frame.addEventListener('pointercancel',finish);
});

// Uncropped source reference while adjusting a frame.
const sourcePanel=document.createElement('aside');sourcePanel.className='source-reference';sourcePanel.hidden=true;
const sourceHeading=document.createElement('div');sourceHeading.className='source-reference-heading';
const sourceLabel=document.createElement('strong');sourceLabel.textContent='完整原始画面';
const closeSource=document.createElement('button');closeSource.type='button';closeSource.textContent='关闭';closeSource.onclick=()=>{sourcePanel.hidden=true;sourcePanel.querySelectorAll('video').forEach(v=>v.pause())};sourceHeading.append(sourceLabel,closeSource);
const sourceViewport=document.createElement('div');sourceViewport.className='source-reference-viewport';
const sourceHint=document.createElement('p');sourceHint.textContent='这里显示完整素材；在书页相框内拖动，调整保留的位置。';sourcePanel.append(sourceHeading,sourceViewport,sourceHint);document.body.append(sourcePanel);
function showSourceReference(frame){const source=frame.querySelector('img,video');if(!source)return;sourceViewport.querySelectorAll('video').forEach(v=>v.pause());const preview=source.tagName==='VIDEO'?document.createElement('video'):new Image();preview.src=source.currentSrc||source.src;if(preview.tagName==='VIDEO'){preview.controls=true;preview.muted=true;preview.loop=true;preview.playsInline=true;preview.addEventListener('loadedmetadata',()=>{preview.currentTime=source.currentTime;preview.play().catch(()=>{})},{once:true})}else preview.alt='未经裁切的完整照片';sourceViewport.replaceChildren(preview);sourcePanel.hidden=false}
editableFrames.forEach(frame=>frame.addEventListener('pointerdown',()=>{if(mediaEditMode==='crop')showSourceReference(frame)},true));
mediaToolbar.querySelectorAll('button').forEach(button=>button.addEventListener('click',()=>{if(button.dataset.mode!=='crop'){sourcePanel.hidden=true;sourceViewport.querySelectorAll('video').forEach(v=>v.pause())}else{const frame=editableFrames.find(f=>!f.closest('.spread').hidden&&f.querySelector('img,video'));if(frame)showSourceReference(frame)}}));

// Minimal per-video heading: date and song, edited directly by the owner.
const danceHeading=document.querySelector('.video-heading');danceHeading.replaceChildren();
const danceDate=document.createElement('span');danceDate.className='dance-date editable-copy';
const danceSong=document.createElement('h2');danceSong.className='dance-song editable-copy';
for(const [el,label,key] of [[danceDate,'日期','dance-date-'],[danceSong,'歌名','dance-song-']]){
 el.contentEditable='plaintext-only';el.setAttribute('role','textbox');el.setAttribute('aria-label',label);el.dataset.placeholder='点击填写'+label;
 el.addEventListener('input',()=>{draftStore[key+clip]=el.innerText;try{void 0;editStatus.textContent='文字已保存在此浏览器'}catch{editStatus.textContent='此浏览器无法保存，请复制备份'}});
 el.addEventListener('keydown',e=>{e.stopPropagation();if(e.key==='Enter'){e.preventDefault();el.blur()}});
 danceHeading.append(el);
}
function loadDanceHeading(){danceDate.textContent=draftStore['dance-date-'+clip]??'';danceSong.textContent=draftStore['dance-song-'+clip]??''}
const setClipWithCopy=setClip;setClip=function(n){setClipWithCopy(n);loadDanceHeading()};loadDanceHeading();

// A quiet, blank note beneath the right half of each video.
const danceNote=document.querySelector('.video-caption p');
danceNote.setAttribute('aria-label','写下这段视频的文字');
function loadBlankDanceNote(){danceNote.textContent=draftStore['dance-description-'+clip]??''}
const setClipWithHeading=setClip;setClip=function(n){setClipWithHeading(n);loadBlankDanceNote()};loadBlankDanceNote();

// Reserve a real writing column beside the video, respecting its source ratio.
const videoSpread=document.querySelector('.video-spread'),videoStage=document.querySelector('.video-stage');
const danceBody=document.createElement('div');danceBody.className='dance-body';videoStage.before(danceBody);danceBody.append(videoStage,document.querySelector('.video-caption'));
const dancePlayer=document.querySelector('#video');
function sizeDanceVideo(){
 if(videoSpread.hidden||!danceBody.clientWidth)return;
 const ratio=dancePlayer.videoWidth&&dancePlayer.videoHeight?dancePlayer.videoWidth/dancePlayer.videoHeight:16/9;
 const small=window.innerWidth<=760,gap=small?16:28;
 const writingWidth=danceBody.clientWidth*(small?.29:.27);
 const availableWidth=Math.max(0,danceBody.clientWidth-writingWidth-gap);
 const width=Math.min(availableWidth,danceBody.clientHeight*ratio);
 videoStage.style.setProperty('--dance-video-width',width+'px');
 videoStage.style.setProperty('--dance-video-height',width/ratio+'px');
}
dancePlayer.addEventListener('loadedmetadata',sizeDanceVideo);
new ResizeObserver(sizeDanceVideo).observe(danceBody);
const setClipWithSideNote=setClip;setClip=function(n){setClipWithSideNote(n);requestAnimationFrame(sizeDanceVideo)};
requestAnimationFrame(sizeDanceVideo);

// Per-frame scaling is independent of the responsive default layout.
document.querySelectorAll('.letter .photo').forEach(frame=>{
 const key=frame.dataset.placementKey,saved=placementStore[key];
 if(saved?.widthPercent){frame.dataset.manualWidth=saved.widthPercent;frame.dataset.manualRatio=saved.ratio;}
 const handle=document.createElement('span');handle.className='frame-resize-handle';handle.tabIndex=0;handle.setAttribute('role','slider');handle.setAttribute('aria-label','缩放这张照片');handle.setAttribute('aria-valuemin','8');handle.setAttribute('aria-valuemax','90');handle.setAttribute('aria-valuenow',saved?.widthPercent||Math.round(frame.offsetWidth/(frame.closest('.letter').clientWidth||1)*100));handle.title='拖动缩放';frame.append(handle);
 let resize=null;
 const persist=()=>{placementStore[key]={...(placementStore[key]||{}),widthPercent:Number(frame.dataset.manualWidth),ratio:Number(frame.dataset.manualRatio)};try{void 0;hint.textContent='尺寸已保存在此浏览器'}catch{hint.textContent='当前浏览器无法保存尺寸'}};
 const scaleTo=(percent,ratio)=>{frame.dataset.manualWidth=Math.max(8,Math.min(90,percent));frame.dataset.manualRatio=ratio;handle.setAttribute('aria-valuenow',Math.round(Number(frame.dataset.manualWidth)));fitMemoryFrames()};
 handle.addEventListener('click',e=>{e.preventDefault();e.stopPropagation()});
 handle.addEventListener('pointerdown',e=>{if(mediaEditMode!=='move')return;e.preventDefault();e.stopPropagation();resize={x:e.clientX,y:e.clientY,w:frame.offsetWidth,h:frame.offsetHeight,angle:(parseFloat(getComputedStyle(frame).getPropertyValue('--collage-angle'))||0)*Math.PI/180};handle.setPointerCapture(e.pointerId);frame.classList.add('dragging-frame')});
 handle.addEventListener('pointermove',e=>{if(!resize)return;e.stopPropagation();const dx=e.clientX-resize.x,dy=e.clientY-resize.y,r=resize.angle,x=dx*Math.cos(r)+dy*Math.sin(r),y=-dx*Math.sin(r)+dy*Math.cos(r);const factor=1+(x*resize.w+y*resize.h)/(resize.w**2+resize.h**2);scaleTo(resize.w*factor/frame.closest('.letter').clientWidth*100,resize.w/resize.h)});
 const finish=e=>{if(!resize)return;e.stopPropagation();resize=null;frame.classList.remove('dragging-frame');persist()};handle.addEventListener('pointerup',finish);handle.addEventListener('pointercancel',finish);
 handle.addEventListener('keydown',e=>{if(!['ArrowUp','ArrowRight','ArrowDown','ArrowLeft'].includes(e.key))return;e.preventDefault();e.stopPropagation();scaleTo(frame.offsetWidth/frame.closest('.letter').clientWidth*100+(['ArrowUp','ArrowRight'].includes(e.key)?2:-2),frame.offsetWidth/frame.offsetHeight);persist()});
});fitMemoryFrames();

// The first class story supplied for dance one.
const firstClassStory="第一次上课的时候大概是我瞎跳了一年舞，认认真真上了三个月爵士之后，因为台风天没课上去上了你的课。那个时候跳舞爱站第一排，感觉老师好严格，一节课纠正我动作n遍，但是也很意外最后居然会有一个被捡的合拍，现在看跳的真惨不忍睹。";
const firstClassRevision='first-class-story-20260914-original';
if(draftStore[firstClassRevision]!==true){draftStore['dance-description-0']=firstClassStory;draftStore[firstClassRevision]=true;try{void 0}catch{}}
const setClipWithFirstClass=setClip;setClip=function(n){setClipWithFirstClass(n);if(n===0)danceNote.textContent=draftStore['dance-description-0']??firstClassStory};
if(clip===0)danceNote.textContent=draftStore['dance-description-0'];

// Original wording supplied for dance two.
const secondDanceStory='这是第一次被你push上去的solo，严重偏台且严重不自信，但那个时候感觉自己跳舞发力的方式改变了，是有在慢慢进步吧';
const secondDanceRevision='second-dance-story-20260914-original';
if(draftStore[secondDanceRevision]!==true){draftStore['dance-description-1']=secondDanceStory;draftStore[secondDanceRevision]=true;try{void 0}catch{}}
if(clip===1)danceNote.textContent=draftStore['dance-description-1'];

// Original wording supplied for dance three.
const thirdDanceStory='这支舞是你修养好膝盖回来不久后学的，这一支可能是我两年k编舞以来唯一满意的一支，当时也没觉得多好看，记忆犹新的是下课被球球夸了，当天晚上发dy还被室友夸了，然后我就复盘了一下，好像确实还可以，甚至目前看了就这支舞算得上是满意的。';
const thirdDanceRevision='third-dance-story-20260914-original';
if(draftStore[thirdDanceRevision]!==true){draftStore['dance-description-2']=thirdDanceStory;draftStore[thirdDanceRevision]=true;try{void 0}catch{}}
if(clip===2)danceNote.textContent=draftStore['dance-description-2'];

// Original wording supplied for dance four.
const fourthDanceStory='这支舞是我最最最喜欢的jealous，这支舞从开新，几乎是一天学一次的，而且也是从这支舞开始，我变得自信了，我开始可以自信的站在c位拍视频了，也可以自愿上去solo了，这次solo其实没有特别冷静，我感觉没有小组跳跳得好，但你说solo跳不好没事，solo是用来练胆量的。所以从那之后，只要有solo的机会，且动作没有记得那么不熟，我都会勇敢走上去。';
const fourthDanceRevision='fourth-dance-story-20260914-original';
if(draftStore[fourthDanceRevision]!==true){draftStore['dance-description-3']=fourthDanceStory;draftStore[fourthDanceRevision]=true;try{void 0}catch{}}
if(clip===3)danceNote.textContent=draftStore['dance-description-3'];

// A balanced loose photo wall. Pack three columns by actual media proportions.
document.querySelectorAll('.letter').forEach(el=>el.classList.add('free-collage'));
function layoutPhotoWalls(){
 document.querySelectorAll('.free-collage').forEach((wall,side)=>{
  const W=wall.clientWidth,H=wall.clientHeight;if(!W||!H)return;
  const layoutKey=W+'x'+H+'|'+[...wall.querySelectorAll('img,video')].map(m=>(m.videoWidth||m.naturalWidth||0)+'x'+(m.videoHeight||m.naturalHeight||0)).join('|');if(wall.dataset.layoutKey===layoutKey)return;wall.dataset.layoutKey=layoutKey;
  const frames=[...wall.querySelectorAll('.photo')];
  const ratios=frames.map(f=>{const m=f.querySelector('img,video');return m&&(m.videoWidth||m.naturalWidth)?(m.videoWidth||m.naturalWidth)/(m.videoHeight||m.naturalHeight):.75});
  // Distribute tall and wide frames across columns to keep each column full.
  const columns=[[],[],[]],heights=[0,0,0];
  [...frames.keys()].sort((a,b)=>ratios[a]-ratios[b]).forEach(i=>{const candidates=[0,1,2].filter(c=>columns[c].length<3);const c=candidates.reduce((a,b)=>heights[a]<=heights[b]?a:b);columns[c].push(i);heights[c]+=1/ratios[i]});
  const gap=Math.max(10,W*.025),margin=Math.max(8,W*.02),columnWidth=(W-2*margin-2*gap)/3;
  columns.forEach((items,c)=>{
   const dimensions=items.map(i=>{const angle=[-3,2,-2,3,-2,2,-3,2,3][i]*(side?-1:1),r=Math.abs(angle)*Math.PI/180;const w=columnWidth/(Math.cos(r)+Math.sin(r)/ratios[i]),h=w/ratios[i];return{i,angle,w,h,bw:w*Math.cos(r)+h*Math.sin(r),bh:w*Math.sin(r)+h*Math.cos(r)}});
   const availableH=H-2*margin-gap*(items.length-1);const scale=Math.min(1,Math.max(0,availableH)/dimensions.reduce((sum,d)=>sum+d.bh,0));
   const total=dimensions.reduce((sum,d)=>sum+d.bh*scale,0)+gap*(items.length-1);let y=(H-total)/2;
   dimensions.forEach(d=>{const f=frames[d.i],w=d.w*scale,h=d.h*scale,bw=d.bw*scale,bh=d.bh*scale;const x=margin+c*(columnWidth+gap)+(columnWidth-bw)/2+(bw-w)/2;
    f.style.setProperty('--layout-x',(x/W*100)+'%');f.style.setProperty('--layout-y',((y+(bh-h)/2)/H*100)+'%');f.style.setProperty('--frame-width',w+'px');f.style.setProperty('--frame-height',h+'px');f.style.setProperty('--collage-angle',d.angle+'deg');y+=bh+gap;
   });
  });
 });
}
document.querySelectorAll('.free-collage img,.free-collage video').forEach(m=>m.addEventListener(m.tagName==='VIDEO'?'loadedmetadata':'load',layoutPhotoWalls));
const photoWallObserver=new ResizeObserver(layoutPhotoWalls);document.querySelectorAll('.free-collage').forEach(w=>photoWallObserver.observe(w));
layoutPhotoWalls();

// Photo-wall lightbox displays only the selected media.
document.querySelectorAll('.free-collage .photo').forEach(frame=>frame.addEventListener('click',()=>{if(mediaEditMode==='view')dialog.classList.add('media-only')},true));
document.querySelectorAll('.moment-photo').forEach(frame=>frame.addEventListener('click',()=>dialog.classList.remove('media-only'),true));
dialog.addEventListener('close',()=>dialog.classList.remove('media-only'));

// Requested single-line header title replaces the old title and subtitle.
const headerTitleRevision='header-title-k-anniversary-20260915';
if(draftStore[headerTitleRevision]!==true){draftStore['header-title']='k编舞两周年纪念册';draftStore['header-subtitle']='';draftStore[headerTitleRevision]=true;try{void 0}catch{}}
brandTitle.textContent=draftStore['header-title'];
brand.querySelector('small').textContent='';brand.querySelector('small').hidden=true;

// A restrained anniversary title page, preserving all editable cover copy.
const openingPage=document.querySelector('.cover-left');
const anniversaryMark=document.createElement('div');anniversaryMark.className='anniversary-mark';anniversaryMark.setAttribute('aria-hidden','true');anniversaryMark.textContent='贰';openingPage.prepend(anniversaryMark);
const anniversarySeal=document.createElement('span');anniversarySeal.className='anniversary-seal';anniversarySeal.textContent='两周年';openingPage.append(anniversarySeal);

// The user's original letter for page seven.
const personalLetterText=`其实有很多话想说，但不知道从哪里说起。我在舞房里一直是一个很i的人，我不会主动跟人讲话，但其实生活里并没有那么内向，所以我觉得是因为我自己在跳舞这件事情上一直都是非常非常不自信的，也没有想过我要跳得有多好，一开始就是为了减肥来的。但自从上了你的课之后，我认识了你，认识了很多你课上的学生，我会想要跟大家一起进步，我有了要把舞跳好看这个想法！虽然距离第一次上你的课已经过去了两年了，但我一开始就是瞎上，也跳不明白编舞，去年6月才正式开始跟着你跑课，但感觉除了你的课之外，我还是跳爵士比较多，所以也会觉得自己进步很慢吧，跳舞也还是不好看。到今年的年初，我才开始慢慢的去接触更多的编舞老师（但其实依旧是今年毕业前夕在p办时限卡之后，才push自己去上课的），让自己去习惯不同的元素和不同的发力，看到的多了，学到的也会更多一点吧，但与此同时也发现了自己跳舞存在的问题，对自己的要求也越来越高了吧。最近这段时间也感觉自己越来越勇敢，拍视频站c开始不紧张了，solo也会push自己上，真的很感谢你给我的能量吧！
虽然说目前我在你的课上跳得不算好（有时候也会后悔自己编舞跳太少了，p办卡办得太晚了，在p跳舞真的进步会更快一点吧哈哈哈哈），但我还是会继续努力的，我希望有一天我也可以跳得很好！当然我也会一直感谢你给过我的帮助！这个k编舞一直跳下去吧！
当然还要特别感谢你是一个很细心的人，你会去关注学生的情绪，让大家开开心心跳舞。应该有很多人感谢过你给予的能量，所以我今天更想说的是，每个人的能量都有限，你完全可以留一部分能量给你自己，我希望你也可以更开心的只做你自己。`;
const personalLetterKey='personal-letter-20260915';
if(draftStore[personalLetterKey]===undefined){draftStore[personalLetterKey]=personalLetterText;try{void 0}catch{}}
const personalLetter=document.createElement('div');personalLetter.className='personal-letter editable-copy';personalLetter.contentEditable='plaintext-only';personalLetter.dataset.copyKey=personalLetterKey;personalLetter.setAttribute('role','textbox');personalLetter.setAttribute('aria-label','想对老师说的话');personalLetter.setAttribute('aria-multiline','true');personalLetter.textContent=draftStore[personalLetterKey];personalLetter.addEventListener('input',()=>saveCopy(personalLetter));personalLetter.addEventListener('keydown',e=>e.stopPropagation());document.querySelector('.letter-right').replaceChildren(personalLetter);

// Apply a two-character indent to every paragraph of the letter.
function formatLetterParagraphs(){
 const paragraphText=personalLetter.children.length?Array.from(personalLetter.childNodes).map(node=>node.textContent).join('\n'):personalLetter.textContent;
 const paragraphs=paragraphText.split(/\n+/).filter(text=>text.trim());
 personalLetter.replaceChildren(...paragraphs.map(text=>{const p=document.createElement('p');p.textContent=text;return p}));
}
formatLetterParagraphs();
personalLetter.addEventListener('blur',()=>{formatLetterParagraphs();saveCopy(personalLetter)});

// A continuous two-page letter, with no inner scrolling.
const letterSpread=document.querySelector('.letter-spread');
const letterHeading=document.createElement('h2');letterHeading.className='letter-spread-heading';letterHeading.textContent='我想对你说';
letterSpread.classList.add('continuous-letter');letterSpread.replaceChildren(letterHeading,personalLetter);
function fitContinuousLetter(){
 if(letterSpread.hidden||!personalLetter.clientHeight)return;
 let size=window.innerWidth<=760?13:16;
 personalLetter.style.fontSize=size+'px';
 while(personalLetter.scrollWidth>personalLetter.clientWidth+2&&size>9){size-=.25;personalLetter.style.fontSize=size+'px'}
}
const letterSizeObserver=new ResizeObserver(fitContinuousLetter);letterSizeObserver.observe(personalLetter);
personalLetter.addEventListener('input',()=>requestAnimationFrame(fitContinuousLetter));personalLetter.addEventListener('blur',()=>requestAnimationFrame(fitContinuousLetter));
requestAnimationFrame(fitContinuousLetter);

// Add the requested signature once, retaining the user's edited letter.
const signatureRevision='letter-signature-jia-20260915';
if(draftStore[signatureRevision]!==true){
 const signature=document.createElement('p');signature.textContent='Jia';personalLetter.append(signature);
 draftStore[signatureRevision]=true;saveCopy(personalLetter);
}
function styleLetterSignature(){const last=personalLetter.lastElementChild;if(last&&last.textContent.trim()==='Jia')last.classList.add('letter-signoff')}
styleLetterSignature();personalLetter.addEventListener('blur',styleLetterSignature);requestAnimationFrame(fitContinuousLetter);

const fromJiaRevision='letter-signature-from-jia-20260915';
if(draftStore[fromJiaRevision]!==true){
 const last=personalLetter.lastElementChild;
 if(last&&last.textContent.trim()==='Jia')last.textContent='from jia';
 else if(!last||last.textContent.trim()!=='from jia'){const p=document.createElement('p');p.textContent='from jia';personalLetter.append(p)}
 draftStore[fromJiaRevision]=true;saveCopy(personalLetter);
}
const previousStyleLetterSignature=styleLetterSignature;
styleLetterSignature=function(){previousStyleLetterSignature();const last=personalLetter.lastElementChild;if(last&&last.textContent.trim()==='from jia')last.classList.add('letter-signoff')};
styleLetterSignature();personalLetter.addEventListener('blur',()=>styleLetterSignature());requestAnimationFrame(fitContinuousLetter);

// Repair paragraph boundaries lost when reading innerText on a hidden book page.
const paragraphFix='letter-paragraph-repair-20260915';
if(draftStore[paragraphFix]!==true){
 let text=Array.from(personalLetter.childNodes).map(n=>n.textContent).join('\n');
 text=text.replace(/\s*(虽然说目前我在你的课上)/,'\n$1').replace(/\s*(当然还要特别感谢你是一个很细心的人)/,'\n$1').replace(/\s*(from jia)\s*$/,'\n$1');
 personalLetter.replaceChildren(...text.split(/\n+/).filter(t=>t.trim()).map(t=>{const p=document.createElement('p');p.textContent=t;return p}));
 draftStore[paragraphFix]=true;saveCopy(personalLetter);
}
styleLetterSignature();requestAnimationFrame(fitContinuousLetter);

// Restore the three exact original paragraphs explicitly requested by the author.
const threeParagraphRevision='letter-three-original-paragraphs-v2';
if(draftStore[threeParagraphRevision]!==true){
 const originalParagraphs=personalLetterText.split(/\n+/).filter(t=>t.trim());
 personalLetter.replaceChildren(...[...originalParagraphs,'from jia'].map(text=>{const p=document.createElement('p');p.textContent=text;return p}));
 draftStore[threeParagraphRevision]=true;saveCopy(personalLetter);
}
styleLetterSignature();requestAnimationFrame(fitContinuousLetter);

// Keep the signature outside the text columns so it cannot flow into another page.
const letterSignatureFooter=document.createElement('div');letterSignatureFooter.className='letter-signature-footer';letterSignatureFooter.textContent='from Jia';letterSpread.append(letterSignatureFooter);
function separateLetterSignature(){
 for(const p of [...personalLetter.children]){
  if(/^(?:from\s+)?jia\s*$/i.test(p.textContent.trim()))p.remove();
  else p.textContent=p.textContent.replace(/\s*from\s+jia\s*$/i,'');
 }
}
separateLetterSignature();saveCopy(personalLetter);
personalLetter.addEventListener('blur',()=>{separateLetterSignature();saveCopy(personalLetter);requestAnimationFrame(fitContinuousLetter)});
requestAnimationFrame(fitContinuousLetter);

document.addEventListener('visibilitychange',()=>{
 if(document.hidden){scheduleWallPlayback(false);document.querySelectorAll('.letter video').forEach(v=>v.pause())}
 else if(page===6)scheduleWallPlayback(true);
});

// Export browser-only edits for publishing the approved version.
if(location.hash==='#export'){
 const exportButton=document.createElement('button');exportButton.textContent='导出当前定稿';exportButton.style.cssText='position:fixed;right:20px;bottom:20px;z-index:100;padding:14px 22px;background:#823f36;color:white;border:0;border-radius:5px;font-size:16px';
 exportButton.onclick=()=>{
  if(document.activeElement instanceof HTMLElement)document.activeElement.blur();
  const payload={format:'kk-memory-final-v1',copy:draftStore,placement:placementStore,exportedAt:new Date().toISOString()};
  const url=URL.createObjectURL(new Blob([JSON.stringify(payload,null,2)],{type:'application/json'}));const a=document.createElement('a');a.href=url;a.download='纪念册定稿.json';a.click();setTimeout(()=>URL.revokeObjectURL(url),10000);
 };document.body.append(exportButton);
}

// Read-only gift edition.
document.querySelectorAll("[contenteditable]").forEach(el=>{el.removeAttribute("contenteditable");el.removeAttribute("role");el.removeAttribute("aria-multiline");el.classList.remove("editable-copy")});
